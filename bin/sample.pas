
	{ 
		Pascal code sample to give some hints on how to
		design the MyPas syntax
	}

{ 
   in a more pedantic version:
   program prog_ID (input, output);
}

program pas_sample_2018_10_10;

	var
	abacaxi, tangerina, pera: boolean;

	x, y, z: real;

	xx, yy, zz: double;

	i, j, k: integer;

	procedure proced_sample (x, y: double; var a, b, c: integer);

		var i, j: integer;

		function func_name (x, y: double): double;

			var i: integer;

			begin
			end;

		procedure A;
			var bla_bla_bla: file;

			function B: text;
				var a: file;
				begin
					blablabla(a)
				end;
			begin
			end;
		begin
		end;
	begin
	end.

